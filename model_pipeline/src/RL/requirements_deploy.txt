# ── Deep learning ─────────────────────────────────────────────────────────────
# Use 2.2.0 as it's the most stable "late-stage" 3.8 support
torch==2.2.0
torchvision==0.17.0

# ── YOLO & RL ─────────────────────────────────────────────────────────────────
# Ultralytics 8.1.0+ often pushes for Python 3.9+
ultralytics==8.0.200
stable-baselines3==2.1.0
shimmy==1.3.0
gymnasium==0.29.1

# ── Backend ───────────────────────────────────────────────────────────────────
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0
websocket-client==1.7.0

# ── Computer vision ───────────────────────────────────────────────────────────
opencv-python-headless==4.8.1.78

# ── ONNX runtime (faster CPU inference for deployed YOLO models) ──────────────
onnx>=1.14.0
onnxruntime>=1.16.0

# ── Frontend ──────────────────────────────────────────────────────────────────
# Docker image uses Python 3.10 — no 3.8 constraint; 1.35+ adds use_container_width to st.image()
streamlit==1.38.0

# ── MLOps tracking ────────────────────────────────────────────────────────────
mlflow==2.9.2

# ── Data ──────────────────────────────────────────────────────────────────────
numpy==1.24.4
pandas==2.0.3

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-client==0.19.0
python-json-logger==2.0.7

# ── Altair charts (already pulled by streamlit, pinned for reproducibility) ───
altair>=4.0.0